# 验证记录

验证日期：2026-08-07（Asia/Taipei）

## 1. 数据与逻辑

- Python 单元测试共 26 项，覆盖：源表契约、缺字段失败、安全除法、质量检查、加权指标、Pareto、五类分层、公开字段、SQL 静态契约、Notebook 无错误输出、Power BI/部署文件契约，以及最终文档的真实性边界与页数。
- 实际管道回执：551 行、551 个唯一账号、8 个品牌、283 个有效账号；品牌消耗与总表对账、分层合计、公开 JSON 字段检查全部通过。
- 数据字典 5 张工作表均完成重建和逐表渲染检查；公式错误扫描未发现 `#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?`、`#N/A`。
- 源“播放成本”字段的反向定义冲突已作为错误写入质量表；图表、网页、DAX 和报告均改用 `spend / plays`。

## 2. 网页功能与响应式

浏览器自动检查覆盖：

- 桌面端初始 KPI 与管道输出一致。
- 方法说明抽屉可打开，并显示单月/脱敏/播放成本审计边界。
- 选择单品牌后 KPI 与图表联动。
- 选择单行动标签后表格与计数联动。
- 390px 宽移动端页面无全局横向溢出；明细表在自己的容器内滚动。
- 页面运行期间未捕获 JavaScript 控制台错误。
- Vitest 共 4 项全部通过；Vite 8.2.1 生产构建成功。

Browser 云端会话对 `localhost` 返回 `ERR_BLOCKED_BY_CLIENT`，因此按前端测试流程使用本地 Playwright/Chromium 完成相同检查。验证结果记录于本地 `qa/dashboard_qa.json`。

## 3. 设计保真记录

| 概念稿要素 | 实现状态 | 调整原因 |
|---|---|---|
| 暖米色画布、深蓝标题、橙色重点 | 保留 | 与业务编辑风格一致 |
| 页签、筛选器、KPI 卡、Pareto 主图 | 保留 | 构成经营总览主路径 |
| 品牌效率对标与行动分层 | 保留 | 分别落到第二、三页 |
| 右侧洞察轨道 | 保留 | 放置集中度、样本与口径提示 |
| 示例中的虚构数字 | 替换 | 只使用管道验证后的真实脱敏结果 |
| 方法审计抽屉 | 新增 | 公开解释单月边界与播放成本冲突 |

## 4. 人工视觉检查

- 桌面经营总览、品牌筛选、行动筛选与移动端截图均已逐张检查。
- 中文字体正常；标题、图例、轴标签、卡片、表格无裁切或重叠。
- 移动端保留清晰阅读顺序；优先操作与方法边界仍可见。

## 5. 文档与报告

- 实操手册：最终 DOCX 渲染为 53 张 A4 页面，逐页检查封面、目录、步骤、表格、完整代码附录、页眉页脚与中文字体；无空白页、重叠或横向裁切。
- 校招简历：DOCX 与 PDF 均为 1 张 A4 页面；姓名、手机号、邮箱、教育时间、两段实习、项目与校园经历均可提取；不存在待填写占位符。
- 分析报告：HTML 重新导出为 6 张 A4 PDF，逐页检查封面、3 张图、表格、方法与边界；PDF 文本层可检索。
- 真实性检查：掌玩经历只写执行工作与主动完成的三次历史周复盘；未写公司正式上线、ROI 提升或工时改善。

## 6. 可复现命令

```bash
export MPLCONFIGDIR="$PWD/qa/matplotlib-cache"
PYTHONPATH=src python -m unittest discover -s tests -v
python scripts/run_pipeline.py
python scripts/build_notebooks.py
python scripts/execute_notebooks.py
node tools/build_data_dictionary.mjs
cd web && npm test && npm run build
cd .. && node tools/visual_qa.mjs
```

## 7. 交付 ZIP 复验

- 压缩包仅包含 83 个仓库文件，不含 `.git`、本地 QA、`node_modules`、`dist`、缓存或个人简历。
- 将 ZIP 解压到全新临时目录后，Python 26 项测试再次全部通过。
- 在解压副本中执行全新 `npm ci` 后，前端 4 项测试与生产构建再次通过，证明压缩包不依赖原工作区文件。
